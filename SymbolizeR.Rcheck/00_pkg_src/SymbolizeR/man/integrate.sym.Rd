% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_integration.R
\name{integrate.sym}
\alias{integrate.sym}
\title{Symbolic Definite Integration}
\usage{
integrate.sym(expr, var, lower = -Inf, upper = Inf)
}
\arguments{
\item{expr}{The integrand (uses non-standard evaluation, no quoting needed)}

\item{var}{The variable of integration (no quoting needed)}

\item{lower}{Lower bound of integration (default -Inf)}

\item{upper}{Upper bound of integration (default Inf)}
}
\value{
Simplified expression of the result OR unevaluated Integrate() call
}
\description{
Computes a definite integral using kernel recognition. Recognizes
Gamma, Gaussian, and Beta distribution kernels and returns their known
normalizing constants. Falls back to an unevaluated Integrate() call if
no pattern matches.
}
\examples{
# Gamma kernel: integral of x^2 * exp(-x) from 0 to Inf = Gamma(3) = 2
integrate.sym(x^2 * exp(-x), x, 0, Inf)

# Gaussian kernel: integral of exp(-x^2) from -Inf to Inf = sqrt(pi)
integrate.sym(exp(-x^2), x, -Inf, Inf)

# Beta kernel: integral of x * (1-x) from 0 to 1 = Beta(2, 2) = 1/6
integrate.sym(x * (1-x), x, 0, 1)
}
