% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_calculus.R
\name{deriv.sym}
\alias{deriv.sym}
\title{Symbolic Differentiation}
\usage{
\method{deriv}{sym}(expr, var)
}
\arguments{
\item{expr}{An R expression (uses non-standard evaluation, no quoting needed)}

\item{var}{The variable symbol to differentiate by (no quoting needed)}
}
\value{
A simplified R expression representing the derivative
}
\description{
Computes the symbolic partial derivative of an expression with
respect to a specified variable. Wraps \code{stats::D} with pre-processing
to handle E() calls and post-processing to simplify the result.
}
\examples{
deriv.sym(x^3, x)             # 3 * x^2
deriv.sym(exp(a * x), x)      # a * exp(a * x)
deriv.sym(x * E(Y), x)        # E(Y) (treats E(Y) as constant)
}
